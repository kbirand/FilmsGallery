# Port to run the server on (default: 3000)
PORT=3000

# Directory containing video files
# For Docker Synology: This sets the HOST path to map to /app/videos
# For Native: This sets the actual directory path to read videos from
VIDEO_PATH=./videos

# Environment (development or production)
NODE_ENV=production
